document.addEventListener('DOMContentLoaded',()=>{let u=session();if(!u||u.role!=='staff')return location.href='../login.html';let bs=get('bookings');if($('#assigned'))renderDash(u,bs);if($('#assignedTable'))renderAssigned(u);if($('#management'))renderManagement(u);if($('#toggle'))renderAvailability(u)});function renderDash(u,bs){let a=bs.filter(b=>b.staffId===u.employeeId||!b.staffId);$('#assigned').textContent=a.filter(b=>b.status!=='Completed').length;$('#completed').textContent=bs.filter(b=>b.staffId===u.employeeId&&b.status==='Completed').length;$('#availability').textContent=(get('staff').find(x=>x.employeeId===u.employeeId)||u).status}function showDetails(bookingId){let b=get('bookings').find(x=>x.id===bookingId);if(!b)return;let p=get('passengers').find(x=>x.id===b.passengerId);let html=`<h2>${b.id}</h2><div class="details-field"><label>Passenger Name</label><div>${p?p.name:b.passenger}</div></div><div class="details-field"><label>Mobile Number</label><div>${p?p.mobile:'N/A'}</div></div><div class="details-field"><label>Service Type</label><div>${b.service}</div></div><div class="details-field"><label>Station</label><div>${b.station}</div></div><div class="details-field"><label>Platform</label><div>${b.platform}</div></div><div class="details-field"><label>Train Number</label><div>${b.train}</div></div><div class="details-field"><label>Date & Time</label><div>${b.date} at ${b.time}</div></div><div class="details-field"><label>Fare</label><div>₹${b.fare}</div></div><div class="details-field"><label>Status</label><div><span class="badge">${b.status}</span></div></div>`;$('#detailsInfo').innerHTML=html;$('#detailsPanel').classList.add('show');$('#detailsPanel').style.display='block'}window.showDetails=showDetails;function closeDetails(){$('#detailsPanel').classList.remove('show');$('#detailsPanel').style.display='none'}window.closeDetails=closeDetails;function renderAssigned(u){function draw(){let q=$('#ssearch').value.toLowerCase(),f=$('#sfilter').value,a=get('bookings').filter(b=>(!b.staffId||b.staffId===u.employeeId)&&(!q||b.id.toLowerCase().includes(q)||b.passenger.toLowerCase().includes(q))&&(!f||b.status===f));$('#assignedTable').innerHTML=a.map(b=>`<div class="listrow"><div><b>${b.id}</b> · ${b.passenger}<br>${b.service} · ${b.station} · ${b.date}</div><span class="badge">${b.status}</span><div>${b.status==='Booked'?`<button class="btn sm" onclick="staffChange('${b.id}','Accepted')">Accept</button> <button class="btn danger sm" onclick="staffChange('${b.id}','Rejected')">Reject</button>`:''}<button class="btn outline sm" onclick="showDetails('${b.id}')">Details</button></div></div>`).join('')||'<p class="muted">No bookings.</p>'}$('#ssearch').oninput=draw;$('#sfilter').onchange=draw;draw()}function staffChange(i,s){let a=get('bookings'),b=a.find(x=>x.id===i),u=session();b.status=s;if(s==='Accepted')b.staffId=u.employeeId;set('bookings',a);toast('Booking updated.');setTimeout(()=>location.reload(),300)}window.staffChange=staffChange;function renderManagement(u){let a=get('bookings').filter(b=>b.staffId===u.employeeId),order=['Accepted','Reached Passenger','Service Started','Completed'];$('#management').innerHTML=a.map(b=>{let i=order.indexOf(b.status),next=order[i+1];return `<div class="listrow"><div><b>${b.id}</b><br>${b.passenger} · ${b.service}</div><span class="badge">${b.status}</span>${next?`<button class="btn sm" onclick="staffChange('${b.id}','${next}')">Mark ${next}</button>`:''}</div>`}).join('')||'<p class="muted">No accepted assignments.</p>'}function renderAvailability(u){let s=get('staff'),m=s.find(x=>x.employeeId===u.employeeId);$('#astatus').textContent=m.status;$('#toggle').textContent=m.status==='Available'?'Set Unavailable':'Set Available';$('#toggle').onclick=()=>{m.status=m.status==='Available'?'Unavailable':'Available';set('staff',s);toast('Availability updated.');renderAvailability(u)}}
